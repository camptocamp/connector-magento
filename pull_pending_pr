set -v
set -e

cd parts/connector-magento
git pull --no-edit origin pull/25/head # Export cancellation of sales orders to Magento
cd -

cd parts/sale-workflow
git pull --no-edit origin pull/109/head # Remove the filter on incoming pickings from the purchase orders
cd -

cd parts/product-kitting
git pull --no-edit origin pull/6/head # Compatibility between sale_dropshipping and sale_bom_split
cd -

set +v
set +e
